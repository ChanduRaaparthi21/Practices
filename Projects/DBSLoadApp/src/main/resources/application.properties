spring.application.name=DBSLoadApp

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/loan_application
spring.datasource.username=postgres
spring.datasource.password=chandu221299
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=your_jwt_secret_key
# 24 hours in milliseconds
jwt.expiration=86400000 

# OAuth2 Configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://your-auth-provider.com

# Logging Configuration
logging.level.org.springframework=DEBUG
logging.level.com.chandu.DBSLoadApp=DEBUG
logging.file.name=logs/loan-application.log

# Server Configuration
server.port=8081

