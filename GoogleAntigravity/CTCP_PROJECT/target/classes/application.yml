spring:
  profiles:
    active: dev
  application:
    name: tfconnect-backend

server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    operations-sorter: method
